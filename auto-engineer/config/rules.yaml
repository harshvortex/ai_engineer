behavior:
  min_delay_minutes: 1
  max_delay_minutes: 10
  skip_probability: 0.0    # Never skip - we want guaranteed activity
  commit_cooldown_hours: 4  # Reduced from 12 so repos can be touched multiple times/day

weights:
  inactivity: 0.5
  readme_quality: 0.2
  dependency_freshness: 0.3
